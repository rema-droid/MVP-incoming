## 2025-05-14 - Standardized Accessible Search Inputs
**Learning:** Search inputs in the app lacked explicit labels and a quick way to clear the query, impacting both accessibility (screen readers) and UX (mobile typing).
**Action:** Always use `sr-only` labels linked to input IDs and provide a 'Clear' button with `aria-label` for all search-related fields.
